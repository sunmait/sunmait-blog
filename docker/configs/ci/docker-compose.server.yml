version: '3.1'
services:
  dev-server:
    image: dev-server
    ports:
      - 5000:3000
      - 49153:49153
    build:
      context: .
      dockerfile: docker/configs/dev/Dockerfile.server
    container_name: ci-server
    networks:
      - mynetwork

networks:
  mynetwork:
    driver: bridge
