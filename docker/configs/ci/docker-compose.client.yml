version: '3.1'
services:
  ci-client:
    image: ci-client
    build:
      context: .
      dockerfile: docker/configs/ci/Dockerfile.client
    container_name: ci-client
    networks:
      - mynetwork

networks:
  mynetwork:
    driver: bridge
